<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Remove Product - Admin Panel</title>

  <style>
    :root {
      --bg: #f4f6f8;
      --panel: #ffffff;
      --border: #e5e7eb;
      --text: #1f2937;
      --muted: #6b7280;
      --header-bg: #1e3a8a;
      --header-text: #ffffff;
      --row-hover: #e6ebf4;
      --row-active: #dbe2f0;
      --danger: #dc2626;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      min-height: 100vh;
      background: var(--bg);
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      color: var(--text);
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 8vh 1rem;
    }

    .panel {
      width: 100%;
      max-width: 560px;
      background: var(--panel);
      border-radius: 14px;
      overflow: hidden;
      box-shadow: 0 12px 30px rgba(0,0,0,0.08), 0 4px 10px rgba(0,0,0,0.05);
    }

    .panel-header {
      padding: 1.7rem 2.2rem;
      background: var(--header-bg);
      color: var(--header-text);
    }

    .panel-header h1 {
      margin: 0;
      font-size: 1.35rem;
      font-weight: 600;
    }

    .panel-header p {
      margin: 0.35rem 0 0;
      font-size: 0.85rem;
      opacity: 0.85;
    }

    .section { padding: 1rem 0; }

    form { margin: 0; }

    button { all: unset; width: 100%; cursor: pointer; display: block; }

    .row {
      padding: 1.15rem 2.2rem;
      transition: background 0.15s ease;
      position: relative;
    }

    .row:not(:last-child)::after {
      content: "";
      position: absolute;
      left: 2.2rem;
      right: 2.2rem;
      bottom: 0;
      height: 1px;
      background: var(--border);
    }

    .row:hover { background: var(--row-hover); }
    .row:active { background: var(--row-active); }

    .row-title { font-size: 1rem; font-weight: 500; }
    .row-desc { margin-top: 0.2rem; font-size: 0.8rem; color: var(--muted); }

    .row.danger .row-title { color: var(--danger); }

    input[type="text"] {
      width: 100%;
      margin-top: 0.3rem;
      padding: 0.5rem;
      border-radius: 6px;
      border: 1px solid var(--border);
      font-size: 0.9rem;
    }

    .back-link {
      display: block;
      text-align: center;
      margin: 1rem 0 0;
      font-size: 0.8rem;
      color: var(--muted);
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <div class="panel">
    <div class="panel-header">
      <h1>Remove Product</h1>
      <p>Enter the product ID to remove it from the catalog</p>
    </div>

    <div class="section">
      <form action="/admin/removeProduct" method="POST">
        <div class="row">
          <label class="row-title" for="productId">Product ID</label>
          <input type="text" id="productId" name="productId" placeholder="Enter the product ID" required />
          <div class="row-desc">
            You can copy the ID from the product page. It appears infront of the product name, for example: 695f37d123cf964eba0e48
          </div>
        </div>

        <button type="submit" style="margin-top: 1rem;">
          <div class="row danger">
            <div class="row-title">Remove Product</div>
          </div>
        </button>

        <a href="/admin" class="back-link">Back to Admin Panel</a>
      </form>
    </div>
  </div>
</body>
</html>
